# Message Flow Visual Diagrams
**Purpose**: Visual representation of how messages flow through the system

---

## 1. SENDING MESSAGE - Complete Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER ACTION                              │
│                    (Types message in UI)                         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  UI LAYER (chat_screen.dart)                                     │
│                                                                   │
│  • Create Message with status: "sending"                         │
│  • Save to database                                              │
│  • Update UI immediately (user sees "sending...")                │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  BLE SERVICE (ble_service.dart)                                  │
│                                                                   │
│  • Validate connection exists                                    │
│  • Get recipient ID (ephemeral or persistent)                    │
│  • Determine encryption method                                   │
│  • Delegate to message handler ───┐                              │
│                                    │                              │
└────────────────────────────────────┼──────────────────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────┐
│  MESSAGE HANDLER (ble_message_handler.dart)                      │
│                                                                   │
│  STEP 1: Connection Validation Ping                              │
│  ┌─────────────────────────────────────┐                        │
│  │ centralManager.writeCharacteristic  │                        │
│  │ (ping data) ← Test if alive         │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 2: Encryption (if contact exists)                          │
│  ┌─────────────────────────────────────┐                        │
│  │ SecurityManager.encryptMessage()    │                        │
│  │ payload = encrypted content         │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 3: Signing (for authenticity)                              │
│  ┌─────────────────────────────────────┐                        │
│  │ SigningManager.signMessage()        │                        │
│  │ signature = sign(message)           │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 4: Protocol Message Creation                               │
│  ┌─────────────────────────────────────┐                        │
│  │ ProtocolMessage(                    │                        │
│  │   type: textMessage,                │                        │
│  │   payload: encrypted content,       │                        │
│  │   signature: signature,             │                        │
│  │   intendedRecipient: recipientId    │                        │
│  │ )                                   │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 5: Fragmentation (BLE MTU limits)                          │
│  ┌─────────────────────────────────────┐                        │
│  │ MessageFragmenter.fragmentBytes()   │                        │
│  │ chunks = [chunk1, chunk2, ...]      │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 6: Set up ACK waiting                                      │
│  ┌─────────────────────────────────────┐                        │
│  │ _messageAcks[msgId] = Completer     │                        │
│  │ _messageTimeouts[msgId] = Timer     │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  ⚡ STEP 7: ACTUAL RADIO TRANSMISSION ⚡                          │
│  ┌─────────────────────────────────────┐                        │
│  │ FOR EACH CHUNK:                     │                        │
│  │   centralManager.writeCharacteristic│ ◄── THIS IS THE RADIO! │
│  │   (device, characteristic, chunk)   │                        │
│  │   WAIT 50ms (prevent GATT overload) │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 8: Wait for ACK or timeout (5 seconds)                     │
│  ┌─────────────────────────────────────┐                        │
│  │ success = await ackCompleter.future │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 9: Return success/failure                                  │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  UI LAYER (updates message status)                               │
│                                                                   │
│  IF success:                                                     │
│    • status = "sent" ✓                                           │
│    • Green checkmark shown                                       │
│                                                                   │
│  IF failed:                                                      │
│    • status = "failed" ✗                                         │
│    • Red X shown, retry available                               │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**✅ VERIFIED**: Actual BLE radio transmission occurs at Step 7

---

## 2. RECEIVING MESSAGE - Complete Flow

```
⚡ BLE RADIO RECEIVES DATA ⚡
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  BLE SERVICE (ble_service.dart)                                  │
│                                                                   │
│  Characteristic Notification Listener:                           │
│  ┌─────────────────────────────────────┐                        │
│  │ centralManager.characteristicNotified│ ◄── RADIO RECEPTION!  │
│  │   .listen((eventArgs) {             │                        │
│  │     final data = eventArgs.value;   │                        │
│  │     handleIncomingData(data);       │                        │
│  │   })                                │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  MESSAGE HANDLER (ble_message_handler.dart)                      │
│                                                                   │
│  STEP 1: Deserialize Chunk                                       │
│  ┌─────────────────────────────────────┐                        │
│  │ chunk = MessageChunk.fromBytes(data)│                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 2: Reassemble Fragments                                    │
│  ┌─────────────────────────────────────┐                        │
│  │ completeMessage =                   │                        │
│  │   _messageReassembler.addChunk()    │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  IF complete message:                                            │
│                                                                   │
│  STEP 3: Parse Protocol Message                                  │
│  ┌─────────────────────────────────────┐                        │
│  │ protocolMessage =                   │                        │
│  │   ProtocolMessage.fromBytes()       │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 4: Routing Decision                                        │
│  ┌─────────────────────────────────────┐                        │
│  │ IF sender == our node:              │                        │
│  │   ❌ BLOCK (echo prevention)        │                        │
│  │                                     │                        │
│  │ IF type == textMessage:             │                        │
│  │   ✅ ACCEPT (direct BLE)            │                        │
│  │   (connection implies intent)       │                        │
│  │                                     │                        │
│  │ IF type == meshRelay:               │                        │
│  │   → Go to Relay Processing          │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 5: Send ACK back                                           │
│  ┌─────────────────────────────────────┐                        │
│  │ onMessageIdFound(messageId)         │                        │
│  │ → Triggers ACK transmission         │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 6: Decryption (if encrypted)                               │
│  ┌─────────────────────────────────────┐                        │
│  │ IF protocolMessage.isEncrypted:     │                        │
│  │   decryptedContent =                │                        │
│  │     SecurityManager.decryptMessage()│                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 7: Signature Verification                                  │
│  ┌─────────────────────────────────────┐                        │
│  │ IF signature exists:                │                        │
│  │   isValid =                         │                        │
│  │     SigningManager.verifySignature()│                        │
│  │   IF !isValid:                      │                        │
│  │     ❌ REJECT (untrusted)           │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 8: Deliver to UI                                           │
│  ┌─────────────────────────────────────┐                        │
│  │ onMessageReceived(                  │                        │
│  │   decryptedContent,                 │                        │
│  │   senderPublicKey                   │                        │
│  │ )                                   │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  UI LAYER (chat_screen.dart)                                     │
│                                                                   │
│  Callback Handler:                                               │
│  ┌─────────────────────────────────────┐                        │
│  │ • Create Message entity             │                        │
│  │   (isFromMe: false, status: delivered)│                      │
│  │                                     │                        │
│  │ • Save to database                  │                        │
│  │   await _messageRepository.save()   │                        │
│  │                                     │                        │
│  │ • Update UI                         │                        │
│  │   setState(() {                     │                        │
│  │     _messages.insert(0, message)    │                        │
│  │   })                                │                        │
│  │                                     │                        │
│  │ • User sees new message! 💬         │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**✅ VERIFIED**: BLE radio reception happens and messages are processed

---

## 3. RELAY PROCESSING - Messages NOT For Us

```
Message Received (from BLE)
         │
         ▼
┌─────────────────────────────────────────────────────────────────┐
│  MESSAGE HANDLER (ble_message_handler.dart)                      │
│                                                                   │
│  Check message type:                                             │
│  ┌─────────────────────────────────────┐                        │
│  │ IF type == meshRelay:               │                        │
│  │   → Call _handleMeshRelay()         │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  RELAY PROCESSING (_handleMeshRelay)                             │
│                                                                   │
│  STEP 1: Extract Relay Data                                      │
│  ┌─────────────────────────────────────┐                        │
│  │ originalMessageId                   │                        │
│  │ originalSender                      │                        │
│  │ finalRecipient                      │                        │
│  │ relayMetadata (hops, TTL, etc)      │                        │
│  │ originalPayload (encrypted content) │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 2: Create Relay Message Object                             │
│  ┌─────────────────────────────────────┐                        │
│  │ relayMessage = MeshRelayMessage(    │                        │
│  │   originalMessageId: ...,           │                        │
│  │   originalContent: ...,             │                        │
│  │   relayMetadata: ...,               │                        │
│  │   relayNodeId: senderPublicKey,     │                        │
│  │   relayedAt: now                    │                        │
│  │ )                                   │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  RELAY ENGINE (mesh_relay_engine.dart)                           │
│                                                                   │
│  Process Incoming Relay:                                         │
│                                                                   │
│  DECISION POINT 1: Is this message for us?                       │
│  ┌─────────────────────────────────────┐                        │
│  │ IF finalRecipient == _currentNodeId:│                        │
│  │   ✅ MESSAGE FOR US!                │                        │
│  │   → Deliver to self                 │                        │
│  │   → Send ACK back                   │                        │
│  │   → Return content to UI            │                        │
│  │                                     │                        │
│  │ ELSE:                               │                        │
│  │   ❓ NOT FOR US - Need to relay     │                        │
│  │   → Continue to decision...         │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  DECISION POINT 2: Should we relay?                              │
│  ┌─────────────────────────────────────┐                        │
│  │ Check #1: Loop Detection            │                        │
│  │ ├─ Have we seen this message?       │                        │
│  │ ├─ Is our ID in relay path?         │                        │
│  │ └─ IF yes: ❌ DROP (loop prevention)│                        │
│  │                                     │                        │
│  │ Check #2: TTL/Hop Limit             │                        │
│  │ ├─ Is hopCount >= maxHops?          │                        │
│  │ └─ IF yes: ❌ DROP (TTL expired)    │                        │
│  │                                     │                        │
│  │ Check #3: Spam Prevention           │                        │
│  │ ├─ Too many from this node?         │                        │
│  │ └─ IF yes: ❌ DROP (rate limit)     │                        │
│  │                                     │                        │
│  │ Check #4: Next Hop Available?       │                        │
│  │ ├─ Do we have connection to forward?│                        │
│  │ └─ IF no: ❌ DROP (no route)        │                        │
│  │                                     │                        │
│  │ ALL CHECKS PASSED:                  │                        │
│  │ ✅ RELAY THIS MESSAGE!              │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 3: Add to Relay Queue ⚠️ THIS IS KEY!                     │
│  ┌─────────────────────────────────────┐                        │
│  │ queuedMessage = QueuedRelayMessage( │                        │
│  │   relayMessage: relayMessage,       │                        │
│  │   nextHopNodeId: nextHop,           │                        │
│  │   queuedAt: now,                    │                        │
│  │   attempts: 0                       │                        │
│  │ )                                   │                        │
│  │                                     │                        │
│  │ _relayQueue.add(queuedMessage) ◄─── STORED HERE!            │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 4: Update Statistics                                       │
│  ┌─────────────────────────────────────┐                        │
│  │ _statistics = _statistics.copyWith( │                        │
│  │   messagesRelayed: +1,              │                        │
│  │   relayQueueSize: _relayQueue.length│ ◄─── TRACKED!         │
│  │ )                                   │                        │
│  │                                     │                        │
│  │ _onStatsUpdated(_statistics) ◄───── UI NOTIFIED!            │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  STEP 5: Return Result                                           │
│  ┌─────────────────────────────────────┐                        │
│  │ RelayProcessingResult.relayed(      │                        │
│  │   nextHopNodeId: nextHop,           │                        │
│  │   reason: "Queued for relay"        │                        │
│  │ )                                   │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**✅ VERIFIED**: Messages not for us ARE stored in `_relayQueue`

**✅ VERIFIED**: Queue size is tracked and UI is notified

**✅ VERIFIED**: Messages are NOT lost

---

## 4. Queue Sync - How It Actually Works (WITHOUT the deleted setter!)

```
┌─────────────────────────────────────────────────────────────────┐
│  MESH NETWORKING SERVICE (mesh_networking_service.dart)          │
│  (This is the BOSS - owns everything)                            │
│                                                                   │
│  Initialization:                                                 │
│  ┌─────────────────────────────────────┐                        │
│  │ _queueSyncManager = QueueSyncManager│                        │
│  │   messageQueue: _messageQueue,      │                        │
│  │   onSendSyncMessage: callback,      │                        │
│  │   onRequestMessages: callback       │                        │
│  │ )                                   │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  Wire to BLE via CALLBACKS (NOT SETTERS!):                       │
│  ┌─────────────────────────────────────┐                        │
│  │ _messageHandler.onQueueSyncReceived =│                       │
│  │   (syncMessage, fromNodeId) async { │                        │
│  │     await _queueSyncManager         │                        │
│  │       ?.handleIncomingSync(...)     │                        │
│  │   }                                 │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ Callback wire
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  BLE MESSAGE HANDLER (ble_message_handler.dart)                  │
│                                                                   │
│  When queue sync message received:                               │
│  ┌─────────────────────────────────────┐                        │
│  │ IF type == queueSync:               │                        │
│  │   onQueueSyncReceived?.call(        │ ◄─── CALLBACK!        │
│  │     syncMessage,                    │                        │
│  │     fromNodeId                      │                        │
│  │   )                                 │                        │
│  └─────────────────────────────────────┘                        │
│                                                                   │
│  ❌ NO SETTER METHOD USED!                                       │
│  ❌ setQueueSyncManager() was NEVER called!                      │
│  ✅ Integration via CALLBACKS only!                              │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

**This is WHY the deleted setter was safe to remove**:
- Queue sync manager is owned by `MeshNetworkingService`
- Integration happens via callbacks
- BLE layer doesn't need to know about queue sync internals
- The setter method was never called and did nothing

---

## Summary Verification Table

| Path | Component | Verified? | Evidence |
|------|-----------|-----------|----------|
| **Sending** | UI creates message | ✅ | `chat_screen.dart:1522` |
| | BLE validates connection | ✅ | `ble_service.dart:1550` |
| | Encryption occurs | ✅ | `SecurityManager.encryptMessage()` |
| | Signing occurs | ✅ | `SigningManager.signMessage()` |
| | Fragmentation occurs | ✅ | `MessageFragmenter.fragmentBytes()` |
| | **RADIO WRITE** | ✅ | `centralManager.writeCharacteristic()` |
| | ACK/timeout | ✅ | `_messageAcks`, `_messageTimeouts` |
| | UI updates | ✅ | `setState()` with status |
| **Receiving** | **RADIO RECEPTION** | ✅ | `characteristicNotified.listen()` |
| | Reassembly | ✅ | `_messageReassembler.addChunk()` |
| | Decryption | ✅ | `SecurityManager.decryptMessage()` |
| | Signature verify | ✅ | `SigningManager.verifySignature()` |
| | UI delivery | ✅ | `onMessageReceived` callback |
| | DB storage | ✅ | `_messageRepository.saveMessage()` |
| **Relay** | Message detection | ✅ | `type == meshRelay` check |
| | Routing decision | ✅ | `finalRecipient != _currentNodeId` |
| | Loop prevention | ✅ | Seen IDs, TTL checks |
| | **QUEUE STORAGE** | ✅ | `_relayQueue.add()` |
| | Statistics tracking | ✅ | `relayQueueSize` updated |
| | UI notification | ✅ | `_onStatsUpdated()` callback |
| **Queue Sync** | Manager creation | ✅ | In `MeshNetworkingService` |
| | **Callback wiring** | ✅ | `onQueueSyncReceived` |
| | Setter method | ❌ | NEVER USED (deleted) |

---

**All key paths verified! The system works correctly and relay messages are NOT lost!** ✅
